import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.X_and_O',
  appName: 'X O',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
